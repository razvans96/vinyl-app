import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dah.vinyl',
  appName: 'vinyl',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
